import styled from "styled-components";

export const MainContainer = styled.main`
  margin: 0 auto;
  /* padding: ${(props) =>
    props.ispadding ? "48px 0 60px" : "48px 12px 60px"}; */
  padding: 48px 0 60px;
  position: relative;
`;
